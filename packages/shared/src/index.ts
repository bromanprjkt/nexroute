// Placeholder paket @nexroute/shared — belum ada kode yang benar-benar dipakai
// bersama antar-paket. `export {}` cuma menandai file ini sebagai modul ES (bukan
// script global) supaya `import '@nexroute/shared'` tidak error selagi masih kosong.
export {};
